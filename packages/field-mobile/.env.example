# Use an HTTPS API origin for production builds. For a physical device in local development,
# use an address reachable from the device rather than localhost.
EXPO_PUBLIC_API_BASE_URL=https://api.example.test
